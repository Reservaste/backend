{
  "name": "@reservaste/domain",
  "version": "0.1.0",
  "private": false,
  "description": "Shared domain types, Zod schemas and pure invariants for the Reservaste platform. Consumed by the frontend app. The database schema (Supabase migrations) lives in this same repo under supabase/, but is not part of this npm package.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
